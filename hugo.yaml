#baseURL: https://example.org/
languageCode: en-us
title: Kenneth Moreland

theme: hyde

params:
  description: Professional Activities

menus:
  main:
    - name: Projects
      url: /projects/

# Allow html tags in markdown.
# This is necessary to make most shortcodes work with markdown.
markup:
  goldmark:
    renderer:
      unsafe: true
